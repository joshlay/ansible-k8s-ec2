apiVersion: apps/v1
kind: Deployment
metadata:
  name: autoscaler
  namespace: autoscaler
  labels:
    app: autoscaler
spec:
  replicas: 1
  selector:
    matchLabels:
      app: autoscaler
  template:
    metadata:
      labels:
        app: autoscaler
    spec:
      containers:
        - name: placeholder
          image: fedora:31
          command: ["/sbin/init"]
